.claude/settings.local.json

# Backup local automatico gravado pelo app a cada save (regenerado; nao versionar)
gerador-os-dados.json
backups/
BACKUP-COMPLETO-*.json
snapshot-bruto-*.json
os-gen-backup-*.json

# Certificados do servidor da fabrica. As CHAVES PRIVADAS moram aqui: quem tem
# o ca.key emite certificado em nome da nossa CA. Nunca versionar.
servidor/tls/
